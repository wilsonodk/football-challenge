{% extends "admin/admin.html.twig" %}

{% block content %}
	{{ parent() }}

	<h3 style="margin-bottom: 5px;">Log Output</h3>
	<div id="log">
	{% for line in log_data %}{{ line|log_style|raw }}{% endfor %}
	</div>
	<p>{{ macros.link("", "Commissioner Homepage", base_admin) }}</p>
{% endblock %}