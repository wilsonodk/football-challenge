{% extends "admin/admin.html.twig" %}

{% block content %}
	{{ parent() }}
	
	<h2>Player List</h2>
	
	<ul id="player-admin">
	{% for player in players %}
		{% set view %}/player/{{ player.uid }}{% endset %}
		{% set edit %}/player/{{ player.uid }}/edit{% endset %}
		{% set del %}/player/{{ player.uid }}/delete{% endset %}
		<li><span class="name">{{ player.name }}</span> <span class="actions">{{ macros.ahref(view, "View", base_admin) }} | {{ macros.ahref(edit, "Edit", base_admin) }} | {{ macros.ahref(del, "Delete", base_admin) }}</span></li>
	{% endfor %}
	</ul>
	
	<p>{{ macros.ahref("", "Back", base_admin) }} or {{ macros.ahref("/player/new", "Create New Player", base_admin) }}</p>
	
{% endblock %}